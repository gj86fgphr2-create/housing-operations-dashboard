# 寓小二数据汇报工作台变更控制

本仓库的工作台结构、统计口径、导出校验、企业微信通知及 GitHub 发布代码均为受保护的稳定功能。

在修改任何代码、模板、流程、配置、依赖、权限或页面结构前，必须先向用户提交以 `【寓小二数据汇报工作台修改】` 开头的具体变更申请，列明修改范围、原因、影响、风险、验证和回滚方案。只有收到用户对该次变更的明确同意后才可修改、部署、提交或推送。

允许无需重复审批地继续执行现有的每小时数据下载、清洗、校验、企业微信发送、生成数据发布以及只读诊断。例行发布只能更新既定生成数据，不得改变代码和统计口径。

